spring:
  application:
    name: "Star Wars Planets"
  data:
    mongodb:
      uri: ${MONGO_URL}
      auto-index-creation: true

server:
  port: 8080

application:
  star-wars-api:
   url: "https://swapi.dev/api/"
   timeout: 3000  # ms
